{% set name = "pytest-robotframework" %}
{% set version = "4.4.0.post2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/pytest_robotframework-{{ version }}.tar.gz
  sha256: 2a1ac0d5712eb0e1afde19ab2c498a21697a2a235912291c80ec9ae81a4996e4

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - pdm-backend
    - pip
  run:
    - pluggy
    - typing_extensions
    - python >={{ python_min }},<4.0
    - pytest >=7,<9
    - robotframework >=6.1,<8.0.0
    - basedtyping >=0.1.0,<0.2
    - ansi2html >=1.9.1

test:
  imports:
    - pytest_robotframework
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/detachhead/pytest-robotframework
  summary: a pytest plugin that can run both python and robotframework tests while generating robot reports for them
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - DetachHead
    - rxm7706
